# Local .terraform directories (plugin, provider, and state info)
.terraform/

# Terraform plan output files
*.tfplan

# Terraform state files (sensitive data)
terraform.tfstate
terraform.tfstate.backup

# Sensitive variable definitions (if any)
*.tfvars
*.tfvars.json

# Crash logs
crash.log

# Ignore override files used for local-only config
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration file (if not shared)
.terraformrc
terraform.rc

# Kubernetes Config File
azurek8s
